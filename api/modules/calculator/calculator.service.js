// eslint-disable-next-line no-use-before-define
export { makeSum, makeSubstraction };

function makeSum({ a, b }) {
  return a + b;
}

function makeSubstraction({ a, b }) {
  return a - b;
}
